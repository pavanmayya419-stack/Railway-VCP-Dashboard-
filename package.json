{
  "name": "vcp-dashboard",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && pip install -r requirements.txt && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.9",
    "tailwindcss": "^4.2.2"
  },
  "dependencies": {
    "lightweight-charts": "^5.1.0",
    "lucide-react": "^1.8.0"
  }
}
